var num = 50;

function logNumber() {
    console.log(num);
    var num = 100;
}

logNumber(); 


// tell the console // topic related -- hoisting
// what happens if we use const instead of var
// how it explains es6 is better than old es5
